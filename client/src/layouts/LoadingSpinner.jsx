const LoadingSpinner = () => (
  <div className="fixed inset-0 z-1000 flex items-center justify-center opacity-75">
        <span className="loading loading-ring loading-xl"></span>
    </div>
);

export default LoadingSpinner;